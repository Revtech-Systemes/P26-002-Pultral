<Page x:Class="P26_002_Pultral.Pages.InternalLogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="InternalLogPage"
      Background="{StaticResource AppBackgroundBrush}">

    <Grid Margin="40">
        <Border Background="#66F8F8FF"
                BorderBrush="{StaticResource LightBorderBrush}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Background="{StaticResource PrimaryBrandBrush}" Padding="24,14">
                    <TextBlock Text="Journal interne"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource OnDarkTextBrush}"/>
                </Border>

                <TextBox x:Name="LogTextBox"
                         Grid.Row="1"
                         Margin="24"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="16"/>
            </Grid>
        </Border>
    </Grid>
</Page>
